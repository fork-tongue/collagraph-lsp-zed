[package]
name = "collagraph-extension"
version = "0.1.0"
edition = "2021"
authors = ["Collagraph Contributors"]
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
zed_extension_api = "0.2.0"

[profile.release]
opt-level = "z"
lto = true
strip = true
